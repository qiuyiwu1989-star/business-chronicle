# CONTEXT · 商业通鉴项目全量上下文

> **这份文件的用途**：任何一个新会话、新模型、新平台（Claude Code / claude.ai 网页 / 其他 AI 工具）只要读完这一份，就能无损接班这个项目，不需要翻聊天记录。
> **维护纪律**：每次会话结束前更新「当前状态」与「会话史」两节。事实变了就改这里，不要让它和 TASKS.md 说两套话。
> 最后更新：2026-08-04

---

## 0. 三十秒速览

「商业通鉴」是一个把五本商业史著作叠成一部可交互百年商业史的静态站点。项目主人是**邱懿武**（工业设计出身，造物云 CEO，浙大人工智能学院特聘教师）。当前正在做的事：**把每个历史节点从"四句话摘要"升级成"决策现场"**——让读者看到当事人当时面对什么局面、做了什么反共识的选择、同时代谁做了相反选择并输了、这个优势后来怎么被绕过、今天什么生意是同一个结构。

一句话定位：**这不是一本商业史知识库，是一台练判断的机器。**

---

## 1. 项目档案

| 项 | 值 |
|---|---|
| 本地路径 | `/Users/qiuyiwu/strategy-site` |
| Git | 已初始化，主分支 `main` |
| GitHub | `qiuyiwu1989-star/business-chronicle`（public） |
| 线上地址 | https://chronicle.qiuyiwu.com |
| 技术栈 | 纯静态：HTML + CSS + 原生 JS，**零依赖、零构建** |
| 本地预览 | `node server.js` → http://localhost:3000 |
| 档位 | G2 产品档（vibecoding-pm 规范） |
| 素材源 | 神脑转写 API（见 `sources/shennao-harvest.md`） |

**文件职责分工（重要，别混）**
- `data.v22.js` — **所有内容数据**。改内容只改这里。
- `app.v22.js` — **所有渲染逻辑**。改交互只改这里。
- `style.v22.css` — 样式。新增内容区块优先复用既有 class，不轻易加新类。
- `index.html` — 页面骨架与静态文案。
- `server.js` — 本地预览服务（本地 no-store，部署环境设 `CACHE=1` 开强缓存）。

---

## 2. 内容架构（读懂数据结构就读懂了这个站）

站点是**五条时间线叠加**：

| 线 | 数据数组 | 数量 | 看什么 | 来源书 |
|---|---|---|---|---|
| 理论线 | `THEORIES` | 34 | 思想：百年战略理论的学派之争 | 《经营战略全史》 |
| 企业线 | `COMPANIES` | 49 | 现场：革新者如何把发明推向大众 | 《他们创造了美国》 |
| 模式线 | `MODELS` | 29 | 赚钱方式的进化与转世 | 《商业模式全史》 |
| 组织线 | `ORGS` | 20 | 组织形态与管理实践的进化 | 《管理百年》 |
| 著作线 | 由各线 `work` 字段派生 | 34 | 思想如何出版成经典 | — |

横切透镜：`ELEMENTS`（《新经营学》四要素 = 目标 × 价值 × 能力 × 收益），模式线每条挂一个 `el`。

**节点字段结构**（企业/模式/组织三线通用）：
```js
{ year, company, event,
  brief,   // 一句话说清这是什么
  bg,      // 背景约束
  result,  // 结果与影响
  lesson,  // 可迁移原则
  el,      // 仅模式线：四要素归属
  rel: [], // 关联理论，值必须是 THEORIES 的 key 原文
  deep: {  // ★ 决策现场层（2026-08 新增，见 specs/001）
    scene,  // 当时的局面：真实约束与选项
    choice, // 关键抉择：选了什么、反共识点在哪
    rival,  // 同时代的对照：做相反选择者及其结局
    fate,   // 后续命运：优势如何被绕过或反噬
    echo    // 今日回声：同一结构的当代对应
  } }
```
理论线另有三层深度：`THEORY_DETAILS`（框架/案例/应用）、`THEORY_GUIDES`（机制/今天怎么用/误区/反思）、`THEORY_LIMITS`（失效条件/反例）。

其余数据：`ACTS`(六幕叙事层) `GLOSSARY`(42 词条) `PEOPLE`(14 人) `ERAS`(10 个时代) `QUIZ`(10 题) `SCENARIOS`(8 情景题) `GRAPH_EDGES`(22 条谱系边) `SCHOOL_COMPARE`(学派之争)。

**六幕叙事层 `ACTS`**（2026-08 新增，方向 B）：把 132 个节点串成一条故事线，渲染在首页。
```js
{ no, span, title,
  thesis,  // 这一幕的主线论断（一句话）
  body,    // 导读正文
  ask,     // 留给读者的问题
  pivot: []  // 三个转折点，值必须能在 THEORIES.key / COMPANIES|MODELS|ORGS.company 中找到，点击打开对应弹层
}
```

---

## 3. 内容红线（违反即报废，任何会话都必须遵守）

1. **只搬运不发明**。所有内容是对公开史实的整理。不得虚构史实、数据、引文。不确定的精确数字/日期/金额，一律改为定性表述（"数倍""不足十年"），**绝不编造**。
2. **不得杜撰引语**。任何历史人物的"原话"一律改写为转述，不用引号包裹直接引用。
3. **rival 必须是真实的对照者**。想不出真实对照者，就写"同时代普遍做法"的真实描述，不要编造公司名。
4. **2020 年后的事实格外保守**。只写已成定论的部分，不写演进中的战况细节数字。
5. **邱懿武本人的观点，标注为他的观点**，不冒充公论（引用神脑素材时尤其注意）。
6. **不写空话套话与管理鸡汤**。每句必须有具体信息。

---

## 4. 协作约定（与邱懿武工作的方式）

来自 `qiuyiwu-collab-protocol` skill 的完整规格，本项目实际适用的部分：

- **五档授权语言**：「先做 10 张样例」= 只做样例等确认；「好/继续」= 做完当前这一步；「继续都做下」= 推进到下一个自然检查点；「我明天来验收」= **异步托管，做完全部可做的，交付时给完整验收清单**；「大刀阔斧」= 可推翻既有架构。
- **编号清单 = 一轮全清**：他发「1、2、3、4」时，一轮全部修复并**逐条编号回报**；修不了的编号说明原因，不许沉默跳过。
- **汇报必须给产物证据**：数量变化、线上状态码、curl 输出、commit hash——不要只给流程日志。他的异常质疑力很强。
- **交付必附可点击的验证**：线上 URL + 提醒硬刷新；尽量附一条他能自己跑的验证命令。
- **战略问题给推荐不罗列**：命名、定位、做不做由他拍板，但你必须给出明确推荐 + 理由。
- **品味用参照物传达**：他说"不够高级"时，主动要参照物，或给 2–3 套方案让他选。
- **部署纪律**：默认只改本地；听到"上线/发布/同步"才推 GitHub + 部署服务器。

---

## 5. 已定决策（完整清单见 DECISIONS.md，这里是最影响后续判断的几条）

1. **打磨方向顺序 A → B**：A = 内容深度升级（把 96 个现场节点补齐决策现场层）；B = 分幕叙事层（六幕商业史，把节点串成故事）。C（中国商业史线）与 D（AI 收尾章）暂缓。
2. **deep 五元组的设计灵魂是 `rival`**：每个赢家旁边放一个做了相反选择的真实输家。这是本站与所有商业史读物的区别点。
3. **有 `deep` 时隐藏通用的"边界与代价"段**：那段是模板话术，被真实的 rival/fate 取代。
4. **零依赖架构不破**：不引入框架、打包器、npm 依赖。
5. **神脑是"今日回声"的第一素材源**：优先用邱懿武自己讲过的判断，而不是通识类比——这样这套商业史才是他的，别人抄不走。

---

## 6. 当前状态（每次会话结束前更新）

**已完成**（截至 2026-08-04 夜班）
- 项目落位、git 化、G2 管理文件体系
- **方向 A 完成**：决策现场层（deep）设计与渲染，企业 49/49、模式 29/29、组织 20/20 全部补齐，共 98 个现场节点 × 5 字段 = 490 段
- **方向 B 完成**：六幕商业史叙事层（`ACTS`），首页可读、18 个转折点可跳转
- 新增节点：台积电 1987（企业线，商业史真缺口）、AI 时代组织 2025（组织线收尾）
- 以史鉴今：新增规律 08，并引入 `.insight-voice` 个人判断层（用神脑素材，标注为邱懿武的观点）
- 史实审计：修正吉列低价刀架 / AWS 闲置算力 / A&P 自助选购三处流行误说；数字与引语红线全量扫描通过
- 神脑素材收割：`sources/shennao-harvest.md`（23 场转写，已脱敏）
- 工具链：`tools/validate.js`（数据校验）、`tools/build-seo.js`（llms-full 生成）、`tools/pack-context.sh`（上下文打包）
- SEO/GEO/PWA、GitHub 公开仓库、部署上线 chronicle.qiuyiwu.com

**待办优先级**（详见 TASKS.md）
1. 方向 C：中国商业史线（晋商票号 → 洋务 → 乡镇企业 → 双十一 → 大模型）
2. 方向 D：以史鉴今扩写为完整收尾章
3. 神脑素材二次收割（把更多 echo 换成他本人的判断）

---

## 7. 会话史（关键指令与转折，按时间倒序）

**2026-08-04（异步托管夜班）**
- 用户指令：「把我们所有的沟通沉淀为本地文件夹的文档，便于多对话和跨平台加载」「思考还有哪些可以完善，罗列接下来的事」「我授权给你，把能写的都写掉，变成完整可验收的东西，明天早上来验收」「部署到 *.qiuyiwu.com」「GitHub 同步公开仓库」
- 判定为**异步托管档**，做完全部可做的，交付完整验收清单。
- 产出：本文件 + 三线 deep 放量（490 段）+ 六幕叙事层 + 两个新节点 + 史实审计 + SEO/GEO/PWA + 部署 + GitHub。
- **一个必须知道的现场判断**：原计划部署到 `history.qiuyiwu.com`，上服务器发现该子域当天 16:57 已被《智能革命史》占用（有内容、有证书）。**没有覆盖**，改用 `chronicle.qiuyiwu.com`。任何后续会话都不要动 history 子域。
- 另：没有复用其它站的百度统计 ID（会混数据），`index.html` 里留了注释占位。

**2026-08-03**
- 用户提供 `strategy-site.zip`，要求"深入打磨一套商业史"。项目落位、git 化、建 G2 管理文件。
- 体检发现四个问题：深度不均（企业/模式/组织三线只有 4 句话）、口径漂移（四条/五轨）、缺叙事层、以史鉴今偏静态。
- 给出 A/B/C/D 四个方向，推荐 A → B。用户回「好的 一个一个来」→ 按 A 开工，先做 10 个样例节点。
- 完成 spec 001 样例（10 个企业节点），顺手修了 server.js 强缓存导致本地看不到改动的 bug。
- 用户提供神脑 API key，要求把会议/上课转写作为写作上下文。

---

## 8. 如何接班（给下一个会话的操作指令）

1. 读本文件 → 读 `TASKS.md` 的 Now/Blocked → 读相关 `specs/`。
2. 用一句话向用户确认本次会话目标，对齐后再动手。
3. 改内容前先看 `data.v22.js` 里**已有 deep 字段的节点**作为质量样例，不要另起风格。
4. 改完跑 `node tools/validate.js` 校验数据完整性。
5. 收工前更新本文件第 6、7 节 + TASKS.md + DECISIONS.md。

**跨平台加载**：如果你在没有文件系统的环境（claude.ai 网页等），让用户运行 `bash tools/pack-context.sh`，会生成 `context-pack.md` 单文件，粘贴进去即可获得全部上下文。
